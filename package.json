{
  "name": "mothers-day-puzzle",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server.mjs",
    "preview": "node server.mjs",
    "check": "node --check app.js && node --check server.mjs && node --check qa-api.cjs && node --check qa-deploy.cjs",
    "qa:deploy": "node qa-deploy.cjs"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "playwright": "^1.59.1"
  }
}
